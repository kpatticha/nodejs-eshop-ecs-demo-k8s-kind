kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: eshop
nodes:
  - role: control-plane
    extraPortMappings:
      # frontend NodePort -> http://localhost:8080 on the host
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
